# ══════════════════════════════════════════════════════════════════════════
# Cloudflare Pages 部署排除清单（gitignore 语法）
# --------------------------------------------------------------------------
# 背景：这个目录既是 git 工作树，又是 Pages 的静态产物根目录。
#   `wrangler pages deploy meltpet` 会**原样上传**目录下的所有文件，
#   而 git 的 .gitignore 只管 git，管不了上传。
#
# 🔴 2026-09-20 发现的真实事故：
#   线上 https://www.meltpet.com/.git/ 曾整个目录公开可读 ——
#   /.git/config（暴露 support@meltpet.com + remote origin）、
#   /.git/HEAD、/.gitignore、/README.md、/90-DAY-PLAN.md、
#   /.claude/settings.local.json 全部返回 200。
#   .git 有 736 个文件 / 21.3MB ⇒ 任何人可下载并还原整站源码历史。
#
# 修法就是本文件：把「仓库里有用的东西」和「该上线的东西」分开。
# 新增任何非站点产物（脚本、配置、文档、编辑器目录）时，记得往这里加一行。
# ══════════════════════════════════════════════════════════════════════════

# ── git 元数据：绝不能上线（源码历史泄露）──
.git
.gitignore

# ── 编辑器 / 工具链目录 ──
.claude
.workbuddy

# ── 运维脚本：从环境变量读密钥，不含硬编码凭证，但没必要公开实现细节 ──
_r2.py
_gen_og.py

# ── 内部文档：README 与运营计划不面向访客 ──
README.md
90-DAY-PLAN.md

# ── 站点自审计产出的临时页面 ──
meltpet-audit-*.html
meltpet-deep-audit-*.html
meltpet-deindexing-*.html
*.temp

# 本文件自身也不上传（它是控制文件，不是资源）
.assetsignore
